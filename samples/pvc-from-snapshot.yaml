apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: from-snapshot
  labels:
    snapshot: demo
spec:
  storageClassName: "" # Empty string for use pv-provisioner
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi